name: Test and update early

on:
<<<<<<< Updated upstream
  push:   
    branches:
      - main   # se ejecuta en cada commit en main
=======
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
>>>>>>> Stashed changes

jobs:
  test-and-merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: Build and run tests
        run: mvn test

      - name: Merge main into early
        if: success()
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git fetch origin early
          git checkout early
          git merge --no-ff main -m "Auto-update early from main after tests passed"
          git push origin early